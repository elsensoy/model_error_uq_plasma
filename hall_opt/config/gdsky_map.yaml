output_dir: "map_results"
run_map: true
reference_data: "${settings.output_dir}/postprocess/output_multilogbohm.json"

map_settings:
  initial_cs: [0.2, 0.1]
  max_iter: 100
  algorithm: 'slsqp'
    
