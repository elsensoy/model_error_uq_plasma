1. Debugging Simulation Failures
-Log the details for failing simulations:
-Parameter values (𝑣1,𝑣2) for simulation.(linear space)
-Retcode and failure reason.
-Simulation configuration.
-Iteration number.

2. Validation Checklist
-Confirm priors and normalization align with simulation's constraints.
-double check the DRAM proposal adaptation frequency (interval) matches the scale of the parameter space.

3.Simplify the Posterior Function
-Minimize simulation checks. 
-Handle retcode or warnings in simulation.py. 
- run_simulation_with_config will either return valid outputs or None in case of failures.(+)
-Focus the posterior function on validating inputs (like priors) and computing the posterior value